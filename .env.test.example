# Test environment settings
NODE_ENV=test
PORT=3001

# Test database (separate from development)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=graphQL_test

# CORS - similar to development for testing
ALLOWED_ORIGINS=http://localhost:4200

# Security settings (can be simplified for tests)
CSRF_SECRET=test-secret-key
JSON_LIMIT=10mb

# Rate limiting (more relaxed for tests)
THROTTLE_TTL=60000
THROTTLE_LIMIT=100